#include <stdio.h>

/**
 * main - Entry point
 * takes no arguments
 *
 * Description: prints a string to standard to standard output using printf
 * Return: return 0 for success
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
